version: "3.8"

services:
  backend:
    build: .
    ports:
      - "9090:8080"
    environment:
      MONGODB_URI: mongodb://mongo:27017/geoshield
      JWT_SECRET: geoshield_secret
    depends_on:
      - mongo

  mongo:
    image: mongo:6
    ports:
      - "27017:27017"
